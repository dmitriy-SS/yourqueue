import styled from 'styled-components';

const Image = styled.img`
  width: 1em;
  height: 1em;
  border-radius: 50%;
`;

const Avatar = () => {
  return <Image src="#" />;
};

export default Avatar;
